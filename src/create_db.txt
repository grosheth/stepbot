# from pymongo import MongoClient

# class Data():

#     def create_db(user, money)
#         for member in ctx.guild.members:
#             print(member.id)
#             cashflow = Wallet.wallet(member.id, 0)
#             print(cashflow)

    # def reset_wallet():
        # connect = MongoClient(CONN_STRING)
        # db = connect.discord
        # collection = db.stats
        # for member in ctx.guild.members:
        #     print(member.id)
        #     money = 0
        #     collection.update_one({
        #     '_id': 	member.id
        #     },{
        #     '$set': {
        #         'Memes': money
        #     }
        #     }, upsert=True)
